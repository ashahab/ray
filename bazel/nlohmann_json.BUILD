cc_library(
    name = "nlohmann_json",
    hdrs = glob([
        "single_include/**/*.hpp",
    ],
    allow_empty = True),
    includes = ["single_include"],
    visibility = ["//visibility:public"],
    alwayslink = 1,
)
